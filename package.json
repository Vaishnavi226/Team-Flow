{
  "name": "team-flow",
  "version": "1.0.0",
  "description": "TeamFlow Platform for Digital Heroes Training Task",
  "main": "index.js",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i assets/css/input.css -o assets/css/tailwind.min.css --minify",
    "build": "npx @tailwindcss/cli -i assets/css/input.css -o assets/css/tailwind.min.css --minify && npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve",
    "dev": "npx @11ty/eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
